version: '3'

services:
  xchain_encoder:
    build: .
    networks: 
      - network1
    ports:
      - "3002:3000"
    logging:
      driver: "json-file"
      options:
        max-size: "30m"
        max-file: "10"
        
networks:
  network1:
    name: xchain_network
    external: true